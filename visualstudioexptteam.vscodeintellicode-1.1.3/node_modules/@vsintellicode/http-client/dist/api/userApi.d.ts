/// <reference types="node" />
import http = require('http');
import { User } from '../model/user';
import { Authentication, ApiKeyAuth } from '../model/models';
export declare enum UserApiApiKeys {
    Bearer = 0
}
export declare class UserApi {
    protected _basePath: string;
    protected defaultHeaders: any;
    protected _useQuerystring: boolean;
    protected authentications: {
        'default': Authentication;
        'Bearer': ApiKeyAuth;
    };
    constructor(basePath?: string);
    useQuerystring: boolean;
    basePath: string;
    setDefaultAuthentication(auth: Authentication): void;
    setApiKey(key: UserApiApiKeys, value: string): void;
    apiV1UserGet(options?: {
        headers: {
            [name: string]: string;
        };
    }): Promise<{
        response: http.IncomingMessage;
        body: User;
    }>;
    apiV1UserModelByModelIdDelete(modelId: string, options?: {
        headers: {
            [name: string]: string;
        };
    }): Promise<{
        response: http.IncomingMessage;
        body: User;
    }>;
}
